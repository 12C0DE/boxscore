import React from "react";
import { Typography } from "@mui/material";

export const Header = () => {
  return (
    <div>
      <Typography variant="h3" mt={4} mb={6}>
        Box Score
      </Typography>
    </div>
  );
};
